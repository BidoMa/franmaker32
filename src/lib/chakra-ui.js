import { extendTheme } from '@chakra-ui/react';

const colors = {
  background: '#0a0a0a',
  foreground: '#ffffff',
  primary: '#10b981',
  'primary-foreground': '#ffffff',
  // Agrega otros colores personalizados aquí
};

const theme = extendTheme({ colors });

export default theme;
